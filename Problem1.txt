

CREATE TABLE User(
  userId CHAR(16) PRIMARY KEY NOT NULL,
  name CHAR(64),
  address CHAR(64)
);

CREATE TABLE Borrower(
  userIdBorrower CHAR(16),
  FOREIGN KEY userId REFERENCES IsA
);

CREATE TABLE Lender(
  userId CHAR(16),
  FOREIGN KEY userId REFERENCES IsA

);

CREATE TABLE Intermediary(
  userId CHAR(16),
  FOREIGN KEY userId REFERENCES IsA

);

CREATE TABLE Loan(TE PRIMARY KEY NOT NULL,
  deadline DATE,
  payDate DATE,
  amount DOUBLE,
  description CHAR(128),
  userId CHAR(16),
  FOREIGN KEY userId REFERENCES requests
);

CREATE TABLE Trust(
  userIdBorrower CHAR(16),
  value INT PRIMARY KEY,
  FOREIGN KEY userIdBorrower REFERENCES Borrower
);

CREATE TABLE Deadline(
  deadline DATE,
  finalDate DATE PRIMARY KEY NOT NULL,
  date DATE,
  FOREIGN KEY date REFERENCES Loan
);

CREATE TABLE Repayment(

);

CREATE TABLE LoanRequest(
  date DA

);

CREATE TABLE Commitment(

);


CREATE TABLE IsA(
  userId CHAR(16),
  FOREIGN KEY userId REFERENCES User

);

CREATE TABLE requests(
  userIdBorrower CHAR(16),
  FOREIGN KEY userIdBorrower REFERENCES Borrower
);








